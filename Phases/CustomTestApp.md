# A Custom Test App for agent

Write by react
Modern Style UI

an input for set the agent id from dataverse
an input for set the key for the agent from dataverse
stream agent response instead of send all in one